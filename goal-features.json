[
  { "id": "F001", "description": "Prisma SQLite foundation: add Patient model + region to Site, db client singleton (src/lib/db.ts), prisma generate + db push, empty seed (0 rows), smoke script proving counts=0", "file": "prisma/schema.prisma, src/lib/db.ts, prisma/seed.ts, scripts/smoke.ts", "passes": true },
  { "id": "F002", "description": "Migrate store.ts (consultations/responses) from JSON files to Prisma queries, async; criteria stays JSON-serialized; all callers awaited", "file": "src/lib/store.ts", "passes": true },
  { "id": "F003", "description": "Migrate site+patient reads (data/sites.ts) to Prisma (async, loadSite->null when absent) + upsertSite/replacePatients helpers; evaluateAllSites async; generator exposes generatePanel(); tests decoupled from DB", "file": "src/lib/data/sites.ts", "passes": true },
  { "id": "F004", "description": "Zero the mock: removed data/site-*.json, consultations.json, responses.json, index.json; gitignored as regenerable dev artifacts; grep proves 0 runtime imports (only OMOP vocab-index reader remains)", "file": "data/, .gitignore", "passes": true },
  { "id": "F005", "description": "Role-selection entry /start (Patrocinador->/sponsor, Site->/site), light Claude theme; landing nav CTA -> /start; Vitest test (routing contract + renderToStaticMarkup hrefs); vitest jsx=automatic", "file": "src/app/start/page.tsx, src/app/start/roles.ts, tests/role-select.test.ts", "passes": true },
  { "id": "F006", "description": "Sponsor feasibility from Python estimator: client.ts (timeout-guarded), /api/feasibility proxy, NationalCard on sponsor page (shows real Observed N=29 from 14 sites + national estimate + honest omop_sample/omop_full fallback). Estimator ran on omop_sample per constraint.", "file": "src/lib/estimator/client.ts, src/app/api/feasibility/route.ts, src/app/sponsor/page.tsx", "passes": true },
  { "id": "F007", "description": "Restyle app shell to light Claude theme: globals.css :root palette remapped to claude.css tokens (--cl-bg paper, --cl-accent terracota, --cl-border sand); btn.primary + privacy banner de-navied; 0 hardcoded navy hex in src (grep-proven); empty-state copy fixed. Verified light on /sponsor + /site.", "file": "src/app/globals.css, src/app/site/page.tsx, src/app/scorecard/page.tsx", "passes": true },
  { "id": "F008", "description": "Confirm parse.ts reads ANTHROPIC_API_KEY from process.env; add .env.local plumbing (placeholder) + verify .gitignore covers .env.local + .env.example", "file": "trialbridge/.env.local, .env.example, .gitignore", "passes": false },
  { "id": "F009", "description": "Green gates + end-to-end smoke: npm run build exit 0, typecheck 0, test all pass; curl estimator returns JSON; final proof pasted", "file": "scripts/smoke.ts", "passes": false }
]

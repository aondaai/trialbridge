# TrialBridge — build progress log

Format: `[step] STATUS feature — note`

- [scaffold] DONE project scaffold — package.json, tsconfig, next.config, vitest.config, .gitignore, features.json created; git init done. Node v22.22.2, npm 10.9.7. Building into ./trialbridge (parent path has a colon+spaces, avoided by using clean subdir).
- [engine] DONE F001-F007,F010 — pure matcher core (types/units/engine/soften/aggregate/feasibility) + hero protocol fixture + 19 unit tests. `npm test` = 19/19 green. Covered: pass/fail/unknown, D3 exclusion-unknown→possible, D5 unit conversion, D2 softening split (fromFail vs fromUnknown), D4 composite-group toggle, <5 suppression, R1/R2 feasibility.
- [infra] FIX colon-in-path — the project path contains `Claude:` which corrupts npm's colon-separated PATH injection → `vitest: command not found`. Fixed by calling bins via explicit ./node_modules/.bin/ paths in package.json scripts.
- [infra] DECIDED stay in place — verified `next dev` (Next 15.5) boots from the colon path and serves HTTP 200. Colon only broke bin-PATH lookup (now fixed); Next itself tolerates it. No relocation. App shell (layout/globals/page) added.
- [data+demo] DONE F008,F009,F011 + service/loader — hybrid seeded generator (3 sites, 220/185/150 pts, mixed lab units→canonicalized, 30-40% HER2 missingness among breast), `npm run demo` prints the full proof: per-site tri-state, aggregate+suppression, HER2 softening split (definite 4→48; +26 genuine / +18 was-unknown caveat), funnel-discounted ~47 enrollable/6mo, <5 suppression fires on definite subgroup. tests 19/19, typecheck clean. `@/` alias resolves under tsx.
- NOTE: verify NCT03529110 + criteria vs ClinicalTrials.gov before pitch (flagged in hero-protocol.ts + demo output).
- [store+ui] DONE F012-F018 — JSON snapshot store (counts-not-rows Response; Prisma schema committed as swap target), seed (B&C responded, A live), shared UI, /sponsor (aggregate+suppression+softening panel), /site (private matcher run + per-patient audit + live submit action), /scorecard (print-CSS), landing, README. English-only, name "Elegível". No frozen-mode branch; no PDF pipeline.
- [verify] DONE — browser-tested the full two-sided loop on the live server (port assigned by preview): Camila's submit wrote site-a live:true → sponsor shows 3 sites, site-a "live" badge, "waiting on" empty. Softening panel interactive: relax HER2 → +44 (split +26 genuine / +18 was-unknown). Then RESET snapshot to B&C-only for the clean demo start.
- [proof] ALL 18 features passing. `npm test` 19/19, `npm run typecheck` clean, `npm run build` OK (4 routes), `npm run dev` boots 200, `npm run demo` prints full pipeline. GOAL END-STATE MET.

## Follow-up (post-goal): parse flow + credibility
- [parse] DONE F019-F021 — the "Marcus posts" beat + Claude in the runtime. src/lib/parse.ts calls claude-opus-4-8 (structured outputs) to turn pasted protocol text into Criterion[]; cached fallback when no ANTHROPIC_API_KEY (ADR 3B). /api/parse + /api/consultations (post auto-seeds all 3 site responses). /sponsor/new: paste->parse->verify/correct low-confidence rows->post; /sponsor accepts ?c=<id>. Browser-verified the full loop (cached fallback path, no key here). 21 tests (added parse fallback), typecheck clean, build OK (6 routes).
- [credibility] DONE F022 — ran an 11-agent adversarial research workflow. NCT03529110 VERIFIED = DESTINY-Breast03 (HER2/ECOG/LVEF are real gates → keep as hero); updated hero-protocol.ts sourceNote. All 5 macro stats have sourcing problems: "86%" is UNVERIFIABLE/misattributed (real=19%, Carlisle 2015), others shaky. docs/citations.md has the defensible "say this instead" line + citation for each. Stats are a PITCH decision, left to the user.
- NOTE for user: the "86%" stat should be dropped from the deck; see docs/citations.md.

## Follow-up: regional breakdown (Brazil)
- [region] DONE F023 — closed the "scorecard exists but no real region breakdown" gap. Added `region` to `SiteMeta` (site-a=Sudeste, site-b=Sul, site-c=Nordeste). New `aggregateByRegion` (matcher/aggregate.ts, same <5 suppression as per-site) + `regionBreakdown` (service.ts, adds R1/R2 feasibility per region). Wired into both surfaces: `/sponsor` gets a "Breakdown by region" card next to the per-site table; `/scorecard?view=brasil` is a new national/printable rollup mode alongside the existing per-site `?site=` view, cross-linked both ways. Browser-verified both routes render correctly (3 regions, no console errors); `npm test` 49/49, typecheck clean, `npm run build` OK (unchanged route count — `/scorecard` handles both modes via `?view=`). CAVEAT noted in-UI: with 1 site per region today, the region rollup is numerically identical to per-site; it becomes a real multi-site aggregate once more sites per region come online.

## Follow-up: CT.gov fetch + OMOP coding layer (PRD v4 P0, first slice)
- [ctgov] DONE — src/lib/ctgov/{types,client,normalize,index}.ts: live fetch from the real ClinicalTrials.gov REST API v2 by NCT id, same live/cached-fallback discipline as parse.ts (ADR 3B) — falls back to the committed hero/nsclc fixtures on failure for those two known NCT ids only, else throws (no fabricated cache for arbitrary trials). Verified live end-to-end in-browser against NCT03529110 — returned the real DESTINY-Breast03 eligibility text via `/api/ctgov`.
- [omop] DONE — src/lib/omop/{types,vocabulary,transform}.ts: pure `toOmopCriteria(Criterion[])` transform, parallel to the matcher (matcher/engine.ts untouched — this doesn't replace synthetic-patient matching, it's the artifact a future OMOP-native matcher needs to query DataSUS/DoctorAssistant per PRD v4). Assertion inferred from kind (inclusion→PRESENT, exclusion→ABSENT — the DoctorAssistant Tier 2 assertion model). Domain/table/vocabulary-family mapped for all 19 fields in use; only `sex`→OMOP Gender (8507/8532) ships a verified numeric concept_id — everything else is honestly `conceptId: 0, needsMapping: true` rather than a fabricated Athena lookup (same discipline as docs/citations.md). Ledger: docs/omop-vocabulary-mapping.md. `/api/omop` route added.
- [ui] DONE — /sponsor/new: "0 · Fetch from ClinicalTrials.gov" (NCT id → prefills protocol text/title, live/cached provenance badge) and "2b · OMOP mapping preview" (per-criterion domain/table/vocabulary/concept/assertion/mapped table) ahead of the existing parse-verify-post flow. Preview only — posting still sends Criterion[] as before, no store/schema changes.
- [tests] tests/ctgov.test.ts + tests/omop-transform.test.ts added. `npm test` 49/49, typecheck clean, `npm run build` OK (8 routes, +/api/ctgov +/api/omop). Browser-verified the full chain live: fetch NCT03529110 → parse → map to OMOP, all three network calls 200, OMOP table renders with correct domain/table/vocabulary/assertion/needsMapping per row.

## Follow-up: live end-to-end test → 2 fragilities found and fixed (F024)
- [test] Ran a real end-to-end intake test with a NON-hero trial (NCT00000102, a real CT.gov study, fetched live) to stress the `/sponsor/new` flow beyond the happy path. Found two real bugs:
  1. **Parse fallback ignored the actual text.** `parseCriteria()` with no `ANTHROPIC_API_KEY` always returned the hardcoded `HERO_CRITERIA`, regardless of what protocol text was passed. Concretely: posted a consultation titled "Congenital Adrenal Hyperplasia..." (real CT.gov title/NCT) whose criteria were silently HER2+ breast cancer — a materially wrong feasibility scorecard, with only a small "📦 Cached parse" badge as a hint.
  2. **`/scorecard` was hardcoded to the hero consultation.** Both the per-site and `?view=brasil` modes called `getConsultation(HERO_META.id)` directly, ignoring any `?c=`. A newly posted consultation was fully visible on `/sponsor?c=<id>` but invisible on `/scorecard` — it always rendered the old hero protocol instead.
- [fix-1] `src/lib/parse.ts`: `parseCriteria(text, nctId?)` now only falls back to a cached fixture (hero or NSCLC) when `nctId` matches that fixture's verified NCT — same "nothing honest to fall back to" discipline as `ctgov/index.ts`. No match → throws instead of substituting the wrong trial's criteria. `/api/parse` catches and returns 422. Client (`sponsor/new/page.tsx`) tracks `textMatchesNct`: true on load/after a ctgov fetch, false the instant the textarea is hand-edited, so a manual paste can't accidentally inherit a stale trusted NCT.
- [fix-2] `src/app/scorecard/page.tsx` now reads `?c=` (falls back to `HERO_META.id` only when absent), for both the per-site and `?view=brasil` modes. All cross-links (`/sponsor` → scorecard, per-site ↔ national) now carry `c=` through a small `scorecardHref()` helper.
- [verify] Re-ran the NCT00000102 case end-to-end: default hero flow unchanged (still returns cached HER2+ criteria, note now names the matched NCT); parsing the mismatched CAH text now throws a clear 422 and the verify table never renders (nothing postable); `/scorecard?c=<posted-id>` and `?view=brasil&c=<posted-id>` both correctly show the posted "Congenital Adrenal Hyperplasia" consultation instead of the hero. `tests/parse.test.ts` rewritten for the new nct-gated signature (+2 new cases for the throw path). `npm test` 60/60, typecheck clean, no browser console errors.
- NOTE: residual known gap, not closed — if a user hand-pastes unrelated text WITHOUT ever using the ctgov fetch step, `nctId` is simply omitted and parse throws (safe), so this doesn't reintroduce the bug; the only remaining trust edge is cosmetic (the "NCT reference" field in step 3 is separately editable and not itself validated against the posted criteria).

## Follow-up: real vocabulary mapping + OMOP data-source plumbing (Phase 2)
Two blockers surfaced trying to go further on the OMOP layer: (1) `athena.ohdsi.org`'s concept-search API returns `403` to unauthenticated/non-browser requests — confirmed live, no public OMOP vocabulary API exists; the OHDSI workflow is a licensed CSV download under your own account. (2) Zero DataSUS/DoctorAssistant connection info anywhere in this repo/environment (grepped everything) — PRD v4 names DataSUS as "DuckDB over OMOP in GCS" but that's a description, not a live endpoint. User decided: download the Athena bundle yourself into `data/vocab/`; OMOP source backend TBD, build the DuckDB/GCS adapter first (PRD's only concretely-named backend) with Postgres/BigQuery as stubs; credentials go in `.env.local`, never chat.
- [vocab] DONE — `src/lib/omop/buildVocabIndex.ts` (pure, tested matching logic: parses the real tab-delimited Athena CONCEPT.csv format, exact-then-substring name match against standard SNOMED/LOINC/RxNorm/Gender concepts) + `src/lib/omop/vocabIndex.ts` (loads `data/vocab-index.json`, returns `null` if absent) + `scripts/build-vocab-index.ts` (CLI wrapper, `npm run build-vocab-index`, prints matched/unmatched honestly). `transform.ts`'s `resolveConcept()` now checks the index first, falls back to Phase 1's `needsMapping: true` default when absent — verified via test that Phase 1 behavior is byte-identical with no index file present. `data/vocab/` gitignored (large licensed data); `data/vocab-index.json` is NOT (small, derived, reviewable). Smoke-tested the CLI end-to-end against a throwaway 3-row fixture (parsed, wrote JSON, reported 0/20 matched as expected for terse Athena names vs. our descriptive conceptNames — mechanism confirmed working, real matches depend on the user's real bundle).
- [datasource] DONE — `src/lib/omop/datasource/`: `OmopDataSource` port (`queryAggregate`/`queryRowLevel`, `AggregateResult.counts` reuses `matcher/engine.ts`'s `CohortCounts` shape so `matcher/aggregate.ts`'s existing <5 suppression needs zero changes once a real adapter is live). `sql.ts` builds a single-round-trip Tier-1 aggregate SQL query against standard OMOP CDM v5.4 tables (person/condition_occurrence/measurement/observation/drug_exposure), pure and fully unit-tested. `duckdbGcs.ts` is the one concrete adapter (`queryAggregate` implemented, `queryRowLevel` throws `NotImplementedError` — Tier 2/DoctorAssistant schema isn't confirmed yet). `stubs.ts` has Postgres/BigQuery placeholders. `mock.ts` is an in-memory `OmopDataSource`, the only implementation actually exercised end-to-end this round.
- [duckdb] DEVIATION from plan: `duckdb` was NOT added as a package.json dependency — `npm install duckdb` failed outright in this environment (`node-pre-gyp: command not found`, same colon-in-path fragility this repo already documents). `duckdbGcs.ts` loads it via a non-literal dynamic `import()` instead, so the rest of the app has zero build/runtime dependency on it; only actually calling `queryAggregate` requires the user to `npm install duckdb` themselves, locally, where a native build can succeed. `npm run build` confirmed clean with duckdb absent entirely.
- [env] `.env.example` added (documents `DATASUS_OMOP_GCS_*` vars, all optional); README gets an "OMOP coding layer" section and a correction — the old "no live EHR/ClinicalTrials.gov fetch" non-goal was stale (CT.gov fetch has existed since the prior follow-up) and has been fixed.
- [scope] Explicitly NOT done this round, by design: Tier 2 row-level query implementation (schema unconfirmed), wiring `OmopDataSource` into any route/UI (nothing to show without real credentials — would be dead code), and integration-testing the DuckDB/GCS adapter against a live DataSUS connection (no credentials exist in this environment; the user will fill `.env.local` and confirm connectivity themselves).
- [tests] `tests/vocab-index.test.ts` + `tests/omop-datasource.test.ts` added. `npm test` 67/67 (up from 60), typecheck clean, `npm run build` OK (still 8 routes — nothing new wired into the app surface).

## Follow-up: enrichment estimator — the deck's "base × joint fraction ± CI" made real (F025)
Context: An Vy Le's "How it works" deck (TrialBridge-Workflow-Explained.pdf) describes the step-4 estimator as prose; it wasn't in the code (the site-matcher, feasibility funnel, and modeledPrevalence multiply-the-marginals path were — grep confirmed no standardize/joint/CI/transportability anywhere). A methodology review of the deck raised three specific weaknesses; this closes all three in `src/lib/enrichmentEstimator.ts` (pure, deterministic, no model in the loop — same discipline as feasibility.ts/modeledPrevalence.ts).
- [crit-1] **Joint fraction gets its OWN interval.** `wilsonInterval(k,n)` — Wilson score (not Wald) because the joint count is small and near 0, where Wald goes negative and understates width. Demonstrated: marginal HER2+ 0.18 [0.146,0.220] rel-width 41% vs JOINT 0.045 [0.029,0.070] rel-width 92% — ~2x wider, and the deck showed the joint bare. This is the number that actually drives the estimate.
- [crit-2] **Multi-covariate standardization + explicit sparse-cell policy.** `standardizeRate(strata,{minStratumN})` does direct standardization over any number of cross-classified strata (the deck's 0.6·0.20+0.4·0.15=0.18 is just the 2-stratum case), propagates Σŵ²·p(1-p)/m variance, and — the answer to "what happens as strata get sparse" — redirects a thin/empty cell's base weight onto the pooled rate (over adequate cells only), flags it, and reports `coveredWeight` so a reviewer sees how much rests on the fallback. Empty cell (n=0) handled without NaN; weight never silently dropped.
- [crit-3] **Transportability is a NUMBER, not a shrug.** `transportabilityBound(interval,tau,max)` widens the sampling CI by a stated bias factor τ, labelled an assumption (not data). São Paulo base 3200: sampling [92,224], +τ=0.3 → [64,291]. `estimateEligible` passes `base` as the ceiling (eligible ≤ base) — caught a real bug where a count band was clamped to [0,1].
- [estimator] `estimateEligible({base,jointK,jointN,tau?,strata?})` = base × fraction ± CI per region; base treated as exact so all count-band uncertainty comes from the fraction; reports raw Wilson AND standardized fraction when strata supplied.
- [tests] `tests/enrichment.test.ts` — 21 cases (Wilson correctness/boundary, joint-vs-marginal width, standardization reweighting, thin+empty cell policy, fallback-widens-interval, transportability containment, base-exact, determinism). `npm test` 88/88 (up from 67), typecheck clean, `npm run build` OK (8 routes — pure lib, nothing wired to the UI yet; that's the obvious next step: surface the joint CI + τ band on /sponsor and /scorecard).

[2026-07-09T18:43] [DONE] [F001] Prisma SQLite foundation: Patient model + region on Site, src/lib/db.ts singleton, db push -> prisma/dev.db, empty seed (0 rows), scripts/smoke.ts proving boot counts=0 + write/readback. Gates green: typecheck 0, test 88/88, build exit 0.
[2026-07-09T18:48] [DONE] [F002] store.ts now Prisma-backed + async (consultations/responses); schema decoupled Response FKs + siteName/heroBottleneckHandle; all 5 callers awaited (sponsor-view async -> sponsor/page await). Gates green: typecheck 0, test 88/88, build exit 0.
[2026-07-09T18:54] [DONE] [F003] sites.ts Prisma-backed + async (loadSite->SiteDataset|null, +upsertSite/replacePatients); service.evaluateAllSites async; generate-data.ts exports generatePanel() + region + guarded main(); scorecard/site pages handle empty/null; nsclc tests decoupled from DB via generatePanel(). Gates green: typecheck 0, test 88/88, build 0, smoke empty+writable.
[2026-07-09T18:56] [DONE] [F004] Removed mock JSON (site-a/b/c.json, consultations.json, responses.json, index.json); gitignored as regenerable. Grep proves 0 runtime imports of mock JSON in src/ (only OMOP vocab-index fs reader remains). Gates green after removal: typecheck 0, test 88/88, build 0.
[2026-07-09T19:00] [DONE] [F005] Role-selection screen /start (light Claude theme, terracota accent verified rgb(217,119,87)); ROLE_OPTIONS routing sponsor->/sponsor site->/site; landing nav CTA -> /start; tests/role-select.test.ts (contract + renderToStaticMarkup); vitest esbuild jsx=automatic. Gates green: typecheck 0, test 90/90, build 0 (/start route emitted). Note: dev-server CSS was stale, fixed by preview restart.
[2026-07-09T19:08] [DONE] [F006] Sponsor connected to Python estimator (FastAPI 8421 over omop_sample per constraint). src/lib/estimator/client.ts (8s timeout, null on offline), /api/feasibility proxy, NationalCard on sponsor page (empty + main states). PROOF: curl /api/feasibility -> observedTotal=29 sitesWithData=14 (real, from 14 proprietary hospitals); national_estimated_n=0 on thin omop_sample, honest note points to omop_full (~4588 per README). .env.example: TB_ESTIMATOR_URL/TB_DATASUS_LABEL. Gates green: typecheck 0, test 90/90, build 0. NOTE: /sponsor still dark-themed (F007 restyles); dev-server CSS stale (cosmetic).
[2026-07-09T19:11] [DONE] [F007] App shell restyled to light Claude theme: globals.css :root palette -> claude.css tokens (paper #FAF9F5, terracota #D97757, sand borders); btn.primary text white, privacy banner terracota-subtle. Grep proves 0 hardcoded navy hex in src/. Empty-state copy fixed (site/scorecard no longer say run db:seed). Verified light theme on /sponsor (terracota brand, Observed 29 green) + /site. Gates green: typecheck 0, test 90/90, build 0.
[2026-07-09T19:12] [DONE] [F008] parse.ts confirmed reading process.env.ANTHROPIC_API_KEY (line 143); grep sk-ant -> 0 hardcoded keys in source; .env.local created with placeholders (compromised key NOT written; user must paste rotated key); git check-ignore confirms .env.local matched by .gitignore:26 .env*.local; not in git status. No tracked-code change -> gates unchanged from F007 (green).
[2026-07-09T19:14] [DONE] [F009] FINAL VERIFICATION — all 7 criteria proven. typecheck 0, test 90/90, build 0. Smoke: DB boot counts 0, write+readback OK. curl /api/feasibility -> observedTotal=29 (real, from Python estimator/DataSUS). grep: 0 mock JSON runtime imports, 0 navy hardcoded. parse.ts key via process.env, .env.local gitignored. Browser end-to-end: /start -> click Sou Patrocinador -> /sponsor (light theme + estimator card Observed 29). Fixed stale .next chunk error (build+dev shared .next) by rm -rf .next + preview restart. ALL 9 FEATURES PASS.

## Follow-up: universal sponsor-intake layer (SourceAdapter registry — separate feature set, tracked in `intake-features.json`)
Goal: widen the front door so a sponsor can bring ANY format; matcher/Criterion untouched. Decisions: no destructive rename of `NormalizedProtocol.nctId` (would break ctgov.test.ts) — generalization lives in a new `ProtocolMeta{sourceId,sourceRegistry}` at the intake layer; ctgov maps onto it. Two lanes: `preParsedCriteria` (structured→verify) / `eligibilityText` (docs→parse.ts). Offline-first, zero new deps.
[2026-07-09T22:22] [DONE] [intake-F001] Intake core landed: `src/lib/intake/{types,registry,index}.ts` (SourceAdapter/IntakeResult/ProtocolMeta/IntakeInput + IntakeRegistry detectBest/ingest) + `adapters/ctgov.ts` (wraps existing fetchProtocol, ZERO new fetch behavior, maps NormalizedProtocol->ProtocolMeta, trust:high). `tests/intake-core.test.ts` (+7: registry selection/zero-score/dup-id/NaN-clamp, ctgov detect + cached-fallback map + unknown-nct throw). Baseline was 134 (not the goal's stale "19"); now test 141/141, typecheck 0.
[2026-07-09T22:29] [DONE] [intake-F002] Envelope extractors, dependency-free + offline: `src/lib/intake/envelope/{zip,pdf,index}.ts`. zip.ts = hand PKZIP central-dir parse + `zlib.inflateRawSync` (methods 0/8 — DOCX/XLSX/eCTD share it). pdf.ts = born-digital text via stream+FlateDecode(`inflateSync`)+`(…)Tj`/`[…]TJ`, Td/TD/T*=newline (NON-goals documented: no OCR, no CID fonts). index.ts = magic-byte dispatch (pdf/docx/text), docxToText pulls `<w:t>` runs. `tests/helpers/fixtures.ts` = real PDF/DOCX/ZIP *writers* (crc32 + deflate) so readers are exercised with zero binary blobs. `tests/intake-envelope.test.ts` +10 (deflate+stored zip round-trip, flate+uncompressed PDF, docx entities, magic-byte sniff, non-DOCX-zip reject). test 151/151, typecheck 0.
[2026-07-09T22:31] [DONE] [intake-F003] `src/lib/intake/locateEligibility.ts`: deterministic offline locator — START_RE (inclusion/eligibility/patient selection/study population headings) → slice → SECTION_END_RE cut after the exclusion block so criteria aren't truncated; verbatim passthrough when no heading. Optional Claude upgrade behind `useLlm`+key, falls back to heuristic on any error. `tests/intake-locate.test.ts` +4 (extract block, no-bleed into Background/Study Design, verbatim fallback, no-exclusion edge).
[2026-07-09T22:31] [DONE] [intake-F004] `src/lib/intake/adapters/document.ts` — the Phase 1 universal funnel: envelope→text→locate→eligibilityText lane (protocol/synopsis/IND-protocol/CSR/pasted all one path; parse.ts + verify table unchanged). detect: text 0.5, pdf/docx-file 0.8, non-docx-zip 0.4 (lets XLSX/eCTD outrank). trust medium (born-digital) / low (headings not found). Registered in defaultRegistry. `tests/intake-document.test.ts` +4 (PDF block extract + no-bleed + filename title, DOCX, synopsis text, detect ordering vs ctgov). test 159/159, typecheck 0.
[2026-07-09T22:34] [DONE] [intake-F005] `src/lib/intake/adapters/fhir.ts` — Phase 2 structured lane: FHIR EvidenceVariable → preParsedCriteria (skips parse.ts, straight to verify). Handles R4 (definitionCodeableConcept+valueX) and R5 (definitionByTypeAndValue); comparator→operator, valueRange→between, valueCodeableConcept→eq, valueBoolean→exists/not_exists; exclude→exclusion. Known field + clean value → confidence 0.9; slugged/unknown → 0.6 (still flagged in verify). trust:high. Fixture `src/data/intakeFixtures.ts` (HER2+ mBC EV, 5 chars). Registered. `tests/intake-fhir.test.ts` +4 (detect EV/Bundle, full mapping incl unit/between/eq/exists, low-conf unknown field, Criterion contract). test 163/163, typecheck 0.
[2026-07-09T22:35] [DONE] [intake-F006] `src/lib/intake/adapters/euctr.ts` — Phase 3 registry breadth (EU CTR / EudraCT YYYY-NNNNNN-CC), ctgov discipline: live best-effort (EU register has no clean JSON API → fetch plain download + locate) then cached verified fixture for a KNOWN id, else throw (no fabricated cache). eligibilityText lane (prose). Registry routes a EudraCT id to euctr, an NCT id to ctgov. `EUCTR_FIXTURE` added. `tests/intake-euctr.test.ts` +4 (detect, registry routing, cached fallback, unknown-id throw). test 167/167, typecheck 0.
[2026-07-09T22:44] [DONE] [intake-F007] `scripts/demo-intake.ts` + `npm run demo:intake`: runs all 4 registered adapters over cached fixtures offline, prints SOURCE|REGISTRY|TRUST|CRITERIA|HOW, exits 1 if any adapter fails. Offline proof (exit 0): ctgov→clinicaltrials.gov high 12 (cached), document→document medium 6 (text→parse+verify), fhir→fhir high 5 (structured), euctr→eudract high 7. `tests/intake-e2e.test.ts` mirrors it (routing + exactly-one-lane + trust). FIXED a PRE-EXISTING test race my new files surfaced: vocab-index.test.ts writes data/vocab-index.json while concept-resolver.test.ts reads it in a parallel worker → set `fileParallelism:false` in vitest.config (sub-second suite; no product code touched). Full suite deterministic 168/168 across 3 runs, typecheck 0.
[2026-07-09T22:56] [DONE] [intake-F008/F009/F010] Phase 4 long tail, all registered + in the demo (7 adapters total). ectd: reuse zip envelope → pick most-protocol-like PDF (m5/ path score) → extractPdfText → locate → eligibilityText, trust low. xlsx: hand SpreadsheetML parse (shared+inline strings, numbers) → structured criteria matrix (kind|field|operator|value|unit → preParsedCriteria, op synonyms, trust medium) OR flatten-to-text fallback (trust low). atlas: ATLAS/OHDSI cohort JSON → approximate (age from DemographicCriteria + InclusionRule names as presence), every row confidence ≤0.6 (honest lossiness), trust medium. Fixtures: ATLAS_COHORT literal + makeXlsx/makeEctd builders. `tests/intake-longtail.test.ts` +5. demo:intake now 7/7 offline (ectd low 6, xlsx structured 3, atlas structured 4). test 173/173, typecheck 0.

## intake FINAL VERIFICATION (2026-07-09T22:57)
ALL 10 intake features passing (intake-features.json 10/10). Gates: `vitest run` 173/173 (22 files; +39 intake tests over the 134 baseline), `tsc --noEmit` clean, `next build` OK (routes unchanged — intake layer is not wired into any route yet; that's the obvious next step), `demo:intake` exit 0 over all 7 registered adapters offline (ctgov/document/fhir/euctr/ectd/xlsx/atlas). Matcher + Criterion + parse.ts contract untouched (added no fields; the two lanes feed the EXISTING verify path). Offline-first honored: no test needs network or a key; ctgov/euctr live calls sit behind try/catch cached fallback, PDF/DOCX/XLSX/eCTD readers are zero-dep (built-in zlib), locateEligibility + all structured maps are deterministic.
Phases landed: 0 (ctgov+paste, pre-existing) · 1 (universal document funnel) · 2 (FHIR structured) · 3 (EU CTR registry) · 4 (eCTD/XLSX/ATLAS long tail). Deferred (next steps): wire `defaultRegistry().ingest()` into `/sponsor/new` behind a file-upload/format-picker so the UI shows the provenance badge + trust-driven verify flagging; live-connect EU CTR parsing (currently best-effort→cached); OCR path for scanned PDFs (documented non-goal today).

## intake code review + Docker deploy + Phase 5 UI (2026-07-10)
- [review] Two independent review agents over the intake layer. Confirmed CRITICAL bug against the engine: an `exists` INCLUSION on a field absent from the patient schema resolves to `fail` → patient excluded (engine.ts:168). ATLAS (every inclusion rule) + FHIR unmapped fallback triggered it → whole cohort silently "excluded". Fixed: those lossy rows map to eq/"yes" so missing → unknown → "possible" (proven via evaluatePatient test). Also fixed: FHIR NaN/one-sided-range no longer high-confidence (one-sided range → gte/lte); locateEligibility section-end regex tightened so exclusion bullets don't truncate the list; document adapter only claims DOCX zips (non-Office zip → cleanly unclaimed); zip/pdf decompression-bomb caps (maxOutputLength 64MB) + malformed-zip bounds checks + ZIP64 guard; DOCX `<w:t>` regex no longer leaks `<w:tbl>`/`<w:tr>` markup; PDF trailing-backslash no longer appends "undefined". Commit 64f3252. Intake suite 44/44, demo:intake 7/7.
- [docker] Local deploy verified via `docker compose` (rebuilt web+estimator from current branch): web http://localhost:3080/start → 200 (`<title>TrialBridge — Choose your role</title>`), estimator :8421 /health → 200, clean logs. Basic-Auth gate correctly disabled locally (no password set). NOTE: the :3000 node process in this env is an unrelated hermes whatsapp-bridge (NOT a TrialBridge dev server) — left untouched. Another session has uncommitted work in this tree (estimator/*.py, constants.ts, metric.ts) — all left untouched; only intake/UI files staged per commit.
- [push] Branch feat/scorecard-reconciliation pushed to aondaai (github.com/aondaai/trialbridge) — new branch, main untouched.
- [phase5] Universal intake UI wired into /sponsor/new. `POST /api/intake` runs the registry over a file upload (multipart) or JSON body (id|text|json). `IntakePanel` = 4 modes (upload/registry-id/paste/structured JSON) + drag-drop dropzone + an 8-format "what can I bring?" showcase with trust chips + provenance badge. Two-lane routing: eligibilityText → parse+verify; preParsedCriteria → straight to verify table. Browser-verified in Docker: registry-id NCT03529110 → provenance badge "Ctgov · api … high trust" + eligibility text populated; `/api/intake` FHIR EvidenceVariable → preParsedCriteria (age gte 18 conf 0.9, brain_metastases exclusion) trust high, skipped the LLM. Commits d9f0bb3. Pushed.
- [next] Remaining polish: surface trust-driven flagging more explicitly in the verify table for structured rows; wire the showcase cards to load sample fixtures; make locateEligibility's START_RE match EU CTR "Principal inclusion criteria" so the euctr live path isn't always cache-fallback.

## Phase 5 code review + polish (2026-07-10)
- [review] Independent agent review of /api/intake + IntakePanel + page wiring. One real SECURITY bug: no raw upload-size cap → unbounded formData()/arrayBuffer() OOM on an unauthenticated route (the zip/pdf caps only bound inflate OUTPUT, not the raw body). Fixed: 413 on Content-Length >25MB + file.size check. SSRF verified safe (id-mode only reaches ctgov/euctr adapters whose detect regexes are anchored; no url input reachable). No XSS.
- [fixes] IntakePanel: double-submit guard + AbortController; file input reset (re-upload same file); keyboard-accessible dropzone (role/tabindex/Enter-Space, aria-label); anchored id hint; non-JSON error tolerance. page: structured imports labeled "🧬 Structured import — no LLM parse" (was mislabeled "📦 Cached parse" — source union widened to include "structured"); clear stale ctResult/provenance between ingests.
- [polish-A] Showcase cards load live samples: "Try a sample →" for CT.gov/EU CTR/FHIR/ATLAS/synopsis (submits the fixture through /api/intake), "Upload your own →" for binary formats (PDF/DOCX/XLSX/eCTD).
- [polish-B] Trust-driven flagging banner in the verify table: TrustChip + "N of M rows flagged for verification", emphasized (amber border) for low-trust sources or any flagged rows.
- [polish-C] locateEligibility START_RE/EXCLUSION_RE now absorb label+adjective prefixes ("E.3 ", "Principal ") so EU CTR "E.3 Principal inclusion criteria" is found → the euctr live path no longer always falls back to cache. +test.
- [verify] Browser-verified in Docker (:3080): ATLAS "Try a sample" → structured lane → provenance badge "Atlas · structured · medium trust" → Step 3 "Structured import — no LLM parse" → banner "4 of 4 rows flagged" → rows use `eq` NOT `exists` (the critical exclude-everyone fix, live). 45 intake tests, typecheck clean, next build OK, web image redeployed. Commit 933858b, pushed to aondaai.

## Site-side EHR intake (CSV/XLSX → Patient[]) — subagent-driven build (2026-07-10)
Mirror of the sponsor intake, for Camila. Spec docs/superpowers/specs/2026-07-10-site-ehr-intake-design.md, plan docs/superpowers/plans/2026-07-10-site-ehr-intake.md. Built via subagent-driven-development: 7 TDD tasks, fresh implementer + reviewer per task, opus final whole-branch review.
- New `src/lib/patient-intake/`: `csv.ts` (dep-free RFC-4180 parser), `types.ts`+`headerMap.ts` (heuristic column→field: Dx→diagnosis, HER-2 Status→her2, Perf Status→ecog, Creatinine (mg/dL)→creatinine, unknown clinical→biomarker), `normalize.ts` (per-field null-on-failure normalizers; labs canonicalized via matcher/units.ts), `csvAdapter.ts`+`registry.ts`+`index.ts` (rows→Patient[] + stats + trust; reuses the sponsor XLSX reader `xlsxRows`).
- `scripts/generate-messy-csv.ts` → committed `data/sample-ehr.csv` (odd headers, g/L hemoglobin, blanks) from the seeded synthetic panel.
- `POST /api/patient-intake` (mirror of /api/intake: 25MB cap, 413/400/422; site-local, no LLM).
- `/site/new` rewritten: upload/paste EHR → mapping table (heuristic pre-fill, correctable dropdowns) → preview + stats + TrustChip → "List site". Fully REPLACES the paste-Patient-JSON path (parsePatientsJson removed; slugify kept).
- Honesty preserved end-to-end: unparseable cell / unmappable column → null/ignored + counted in stats → matcher `unknown` → "possible", never wrongly excluded.
- Reviews: every task spec✅+quality-approved; Task 5 Important fix (Sex column F/M); final review Approved-to-merge with fix wave bf5c254 (dup-id write crash → ensureUniquePatientIds; dead const; trust=low on 0 columns). Full suite 216/216, tsc clean, next build OK.
- E2E verified live in Docker (:3080): paste CSV → "3 rows · 11 mapped · 0 ignored · 2 unknown" + medium trust + mapping table → submit → site + 3 patients written; DB round-trip lossless (hgb g/L→g/dL, her2 neg→negative + blank→null, stage "Stage II"→"II").
- Commits: f2c7064..bf5c254 (on feat/scorecard-reconciliation).

## Site EHR intake — review-fix wave 2 (2026-07-11)
Two-agent adversarial review of the final feature state surfaced bugs the per-task + first whole-branch review missed. All fixed in 45e8f75 (full suite 218/218, tsc clean, next build OK; live-smoked in docker):
- CRITICAL: the mapping-override was keyed by column HEADER TEXT, so two columns sharing a header (or two blank headers) collapsed — editing one dropdown retargeted both. Now keyed by column INDEX (`Record<number,MapTarget>`), route→adapter→panel consistent; the existing adapter override test updated to `{8:"ignore"}`.
- IMPORTANT: `csvAdapter.assign()` wrote null unconditionally, so a second column mapping to the same field with a blank cell overwrote an earlier good value (ECOG=1 + blank Perf Status → ecog null). Now writes only on success (guards mirror the id/diagnosis cases). +test.
- IMPORTANT: `reMap` had no busy-guard/abort → a stale response could revert a correction; and "List site" wasn't gated on a pending re-map. Added AbortController + busy guard + `<select disabled={busy}>`, and lifted `busy` to the page via `onBusyChange` to gate the submit button.
- MINOR: `.txt` uploads (advertised in `accept`) now accepted by the adapter.
- Ship-as-is: streamed-body size cap matches the existing /api/intake posture (content-length + post-read file.size).

## Feasibility Autofill (autonomous /goal run)

- [2026-07-11T03:36Z] DONE F0-1 — 11 Prisma models added (siteId-scoped), db push synced clean, all 3 gates green (tsc 0, 218 tests, next build). Baseline was green before changes.
- [2026-07-11T03:41Z] DONE F0-2 — seeds/questionbank.seed.json (16 sections, 5 catalog rows, 32 labels) + scripts/seed-autofill.ts; idempotent (templates=1, catalogRows=5 on re-run); 3 gates green.
- [2026-07-11T03:43Z] DONE F0-3 — classifier label fixture (32 typed labels, A:10 B:14 C:4 D:4) + integrity test; 223 tests green. EPIC F0 COMPLETE.
- [2026-07-11T03:47Z] DONE F1-1 — feasibility form ingestion (reuses intake envelope; parseFormText/ingestForm → FormFieldDraft[]; canonical-template recognition + order-independent fingerprint); 7 tests; 230 green.
- [2026-07-11T03:51Z] DONE F1-2 — archetype+concept classifier (synonym→code→shortlist→unmapped; word-boundary synonym match; keyword section rules). P/R test vs 32 labels ≥0.85 acc, per-archetype P/R ≥0.7. 238 tests green. EPIC F1 COMPLETE.
- [2026-07-11T03:54Z] DONE F2-1/F2-2 — profile (A) + capability (B) resolvers; pure row→Metric (SITE_DECLARED), thin Prisma binding separate; unavailable-not-blank on misses; 8 tests; 246 green.
- [2026-07-11T03:57Z] DONE F2-3/F2-4 — dependency-free DOCX writer (zipArchive CRC32+deflate, fillDocxTemplate, makeDocx; round-trip preserves rPr styling, reproducible bytes) + render diff guard (only approved ships; edited/proposed/rejected withheld; end-to-end test proves LLM draft never reaches DOCX). 259 tests green. EPIC F2 COMPLETE.
- [2026-07-11T04:00Z] DONE F3-1 — cohort resolver (archetype C) reuses matcher engine+soften+suppress; candidate N as MODELED Metric, per-criterion softening deltas, <5 suppression preserved, aggregate-only (no rows). 264 tests green. EPIC F3 COMPLETE.
- [2026-07-11T04:05Z] DONE F4-1/2/3 — RAG retrieval (pure keyword+concept), narrative resolver (parse.ts pattern, always status=proposed, MODELED/LOW, cached-template fallback), review workspace route /site/feasibility + HITL invariant (D never auto-approved; bulk-approve excludes D). Route verified: HTTP 200, 16 sections + A/B/C/D chips render, no runtime errors. 277 tests green. EPIC F4 COMPLETE.
- [2026-07-11T04:07Z] DONE F5-1/2/3 — Kahn DQ flags (conformance/completeness/plausibility off catalog completeness), audit diff+versioning (injected timestamps), learning loop (synonym write-back + approved-narrative RAG index). 11 tests; 288 green. EPIC F5 COMPLETE.
- [2026-07-11T04:09Z] DONE F6-1/2 — scorecard feed (answers→Metrics, reuses src/lib/metric, passes provenance gate; provenance index by seal) + inbox (view over Consultation/Response, no parallel dispatch table). 293 tests. EPIC F6 COMPLETE — ALL F0–F6 DONE.
- [2026-07-11T11:33Z] DONE ADR-002 M0 — cohort.preview MCP tool: dependency-free stdio JSON-RPC server on the site boundary, wraps existing resolveCohort, returns aggregates only + patient-leak guard. 9 tests; 302 green. Server smoke-tested (initialize/tools/list/tools/call).
- [2026-07-11T11:48Z] DONE ADR-002 M1 — orchestrator (routes A/B/C/D → provenanced answers; cohort via injected MCP tool once; D via injected drafter, always proposed; provenance gate on assembly) + MCA agent definition (declares only cohort.preview MCP tool). 8 tests; 310 green.
- [2026-07-11T13:12Z] DONE code-review wave — fixed 12 findings from 3 review agents: cohortServer null-crash guard; leak guard exact-leaf-match (no substring false-positives); note sub-count suppression; LOINC context gate; seed completeness decimal fix + no-clobber re-seed; approveAnswer bars automated actors for D; siteId denormalized on request-owned rows (+indexes, +uniques); empty profile→unavailable; stable RAG id; audit create-case + order-insensitive diff. 315 tests green; tsc/build 0. ZIP writer verified correct (independent unzip -t + Python zipfile).
- [2026-07-11T13:38Z] DONE ADR-002 M2 — narrative critic (adversarial grounding: heuristic flags ungrounded drafts + unsupported numbers/counts; injectable Claude refuter; falls back safe). Wired into orchestrator: every D answer carries a critique, advisory-only, never changes status. 6 tests; 321 green.
- [2026-07-11T13:41Z] DONE ADR-002 M3+M4 — network fan-out (bounded concurrency, per-site failure isolation, aggregate-only network summary excluding suppressed) + nightly freshness scheduler (stale-catalog planner, low-completeness half-window, bounded, clock-free). ADR rollout table updated: M0–M4 all built/offline-tested. 331 tests green.
- [2026-07-11T14:21Z] DONE ADR-002 live seam — integration A (buildLiveDeps: Prisma + MCP client + Claude/template, runnable via `npm run autofill:run`, gated behind ANTHROPIC_API_KEY) + real MCP stdio client (round-trip tested offline vs the cohort server subprocess) + integration B scaffold (managedSession.ts, MCA create-agent/session, VERIFY-tagged). Orchestrator cohort now tolerant (offline cohort → C unavailable, not a crash). No-key run verified end-to-end: 16 provenanced answers, A from real profile, C graceful, critic live. 334 tests; gates 0.
- [2026-07-11T14:31Z] VERIFIED live keyed run (user-authorized key from .env.local) — integration A end-to-end with real Claude: A from seeded profile, C graceful, D drafted by Claude grounded in 2 RAG exemplars, and the M2 critic CAUGHT a real logical contradiction + an overclaim in the draft (grounded=false), all status=proposed. Added db:seed-demo-prior helper.
- [2026-07-11T14:59Z] DONE 2nd code-review wave — fixed MCP client robustness (per-request timeout, EPIPE-safe stdin, closed-on-exit, stderr inherit no-deadlock, detached spawn + process-group kill → 0 orphans verified), buildLiveDeps closes on init failure, fanout surfaces cohortUnavailableSites + per-site error, critic skips years + exact numeric-token match, scheduler JSON-safe finite age sentinel, managedSession sendEvent ok-check. 336 tests (+2 client). Secrets scan clean.
- [2026-07-11T15:17Z] DONE design-system adoption — imported TrialBridge Design System (DesignSync MCP) and brought /site/feasibility on-brand: ported MetricChip provenance seal (tokens --tb-prov-* + .tb-chip--{prov} + .tb-arch) into claude.css; added MetricChip + ArchetypeTag to ui.tsx wired to metric.ts Provenance; rewrote the page with cl-/tb- classes, PrivacyBanner, serif display, terracota/paper. Verified in browser (light). 336 tests; gates 0.
- [2026-07-11T21:10Z] DONE design review fixes — tooltip escapes .cl-card (overflow visible on legend); aria-describedby links MetricChip tip; dark .tb-arch--C lifted to match its chip; raw px font-sizes tokenized. 336 tests; gates 0.
- [2026-07-11T21:23Z] DONE roadmap #1 — live review workspace. persist.ts (orchestrator run → FormField/FieldAnswer + DQ); seed-demo-request.ts (demo request + 8 patients, in-process orchestration, template D, cohort n=7); page renders live answers by section with MetricChip seals, DQ + status badges, D draft + M2 critique; actions.ts HITL server actions (approve/reject/edit + bulk approve high-confidence) via review.ts + audit. Verified in-browser: bulk approve moved 0→4/16 (D + low-conf correctly excluded). 336 tests; gates 0.
- [2026-07-11T21:51Z] DONE FIN-1/FIN-2 — US-1 upload (intakeRequest.ts buildRequestDraft + createFeasibilityRequest; /api/feasibility-intake route; IntakePanel + inbox) and US-2 autofill (runAutofill action + shared buildInProcessDeps; persist split into persistIntakeFields/persistAnswers). Seed DRYed onto buildInProcessDeps. 339 tests; gates 0.
- [2026-07-11T21:53Z] DONE FIN-3 — US-5 export. render/exportDocx.ts buildExportDocx (approved-only via diff guard, no leftover tokens) + GET /site/feasibility/export?req route serving the .docx; workspace export button. Test: approved values ship, proposed/edited/rejected withheld. 341 tests; gates 0.
- [2026-07-11T21:58Z] DONE FIN-4 — US-6 learn wired. ConceptSynonym model; classify.ts consumes learned synonyms (buildMatchers + optional map); orchestrator threads learnedSynonyms; learnPersist.ts (loadLearnedSynonyms/persistLearnedSynonym/persistApprovedNarrative); actions — runAutofill loads learned synonyms, approving D indexes a PriorFormAnswer, mapConcept persists a synonym. Test: learned synonym maps a previously-unmapped label. 344 tests; gates 0.
- [2026-07-11T22:00Z] DONE FIN-5 — ontology extended to ASCVD/IBD. classify.ts CONCEPT_SYNONYMS gained stroke/pad/t2dm/hypertension/ckd/heart_failure (split from comorbidities) + PT-BR/ICD synonyms + LDL/HDL/HbA1c/PCR + estatina. ASCVD/IBD B-field P/R test ≥0.8. No fabricated concept_ids (slugs, not OMOP ids; concept-map.json untouched). 347 tests; gates 0.
- [2026-07-11T22:04Z] DONE FIN-6/FIN-7 — round-trip test (isolated site, cleanup; asserts only-approved-ships + no-patient-id-leak) + autofill:e2e driver (docx→request→autofill→approve→export headless, prints summary). 350 tests; gates 0. ROUND-TRIP COMPLETE (US-1/2/5/6 + #4).
- [2026-07-11T22:22Z] DONE FIN code-review fixes — tenant-scoped every export/action query by SITE (cross-tenant hardening); learn indexes the current (edited) D value not the stale draft; refill writes an autofill-reset audit; intake 400 on bad JSON; roundtrip asserts cohort===5 + label:value-precise leak check; docstring nit. Privacy/regex/orderIdx/export confirmed correct by review. 350 tests; gates 0.
- [2026-07-12T14:27Z] MCA integration B — TESTED LIVE. Installed SDK 0.110.0 exposes real Managed Agents API (beta.environments/agents/sessions/events + cloud & self_hosted env types). scripts/mca-smoke.ts (npm run mca:smoke) creates cloud env → agent → session → sends message → agent replied "MCA online." → cleaned up. Account HAS MCA access. Rewrote managedSession.ts from the guessed fetch scaffold to the real SDK (createEnvironment/Agent/startSession/sendStudy/readReply/runManagedAutofill) wired to cohort.preview via {type:url} MCP tunnel. Remaining gate: MCP-tunnel preview + reachable site server. 350 tests; gates 0.
